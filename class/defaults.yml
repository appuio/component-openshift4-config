parameters:
  openshift4_config:
    =_metadata:
      multi_tenant: true
    globalPullSecrets: {}
    caBundle: null

    images:
      oc:
        registry: quay.io
        repository: appuio/oc
        tag: v4.18

    motd:
      messages: {}
      include_console_notifications: false

    etcdCustomization:
      enabled: false
      spec:
        controlPlaneHardwareSpeed: ""
